#Notes from the Helsinki Website
print("hello") #simple print command with a string of characters.
print('"hey bitch," she said.') #simple print command with simple quotations to include quotation marks in the string.

name = input("What's your name?") #this is a variable containing information given by the user through an input prompt, in this case both are name.
print("Hello, " + name) #print command combined with previous stored info in the name variable, must be outside string.

name = input("What is your name?")
print("!" + name + "!" + name + "!") #wtf is this. Jk basically you can refer to the same variable multiple times

#You can also have multiple inputs (variables) and print them together. If you input information into a variable after it had already been assigned a value, it replaces the previous information. Examples will follow later.
name = input("What is your name?")
lastname = input("What is your last name?")
address = input("What's your address?")
citypostal = input("What city and postal code?")
print(name + " " + lastname)
print(address)
print(citypostal)

#Telling a story of your choosing I guess
name = input("Please type in a name:")
year = input("Please type in a year:")

print(name + " is a valiant knight, born in the year " + year + ". One morning " + name + " woke up to an awful racket: a dragon was approaching the village. Only " + name + " could save the village's residents.") #REMEMBER THE SPACES AND PUNCTUATION!!!


#Variables are needed for various purposes in programming. You can use variables to store any information that will be needed later in the program's execution. You store values in variables.

#It is often useful to name variables according to what they are used for. For example, if the variable contains a word, the name word is a better choice than, say, a.

#There is no set limit to the length of a variable name in Python, but there are some other limitations. A variable name should begin with a letter, and it can only contain letters, numbers and underscores _.

#The variables name, Name and NAME are all different variables.This rule has a few exceptions.

#You can have integers and strings but they can't do the same operations.





# F string example (I fixed this code from print commands with strings to f strings.

name = "Tim Tester"
age = 20
skill1 = "python"
level1 = "beginner"
skill2 = "java"
level2 = "veteran"
skill3 = "programming"
level3 = "semiprofessional"
lower = 2000
upper = 3000

print(f"my name is {name}, I am {age} years old")
print()
print("my skills are")
print(f" - {skill1} ({level1})") #REMEMBER THE SPACES BEFORE THE DASHES HERE
print(f" - {skill2} ({level2})")
print(f" - {skill3} ({level3})")
print()
print(f"I am looking for a job with a salary of {lower}-{upper} euros per month")

